# Settings Scripts

I want to convert the contaner based styling in grids.css to Lucee logic.

There is a component grids.cfc with a css() where the logic will go.

The end product of the method should be css that recreates the container based styling without the CSS vars.

NOte that the container based styling uses an additional DIV, gridInner. This won't be used in the script based version;

